<link rel="stylesheet" href="/assets/css/pages/seller-dashboard.css">
<link rel="stylesheet" href="/assets/css/pages/courier-dashboard.css">

<div class="panel-layout">
    
    <%- include('../partials/sidebar') %>

    <main class="panel-content">
        <!-- Kurye Durum Göstergesi -->
        <div class="courier-status-indicator" id="courier-status-indicator" style="position: fixed; top: 1rem; right: 1rem; z-index: 1000; display: flex; align-items: center; gap: 0.5rem; padding: 0.5rem 1rem; background: var(--card-bg); border-radius: 25px; box-shadow: var(--shadow-md); border: 2px solid var(--border-color);">
            <span class="status-dot" id="status-dot" style="width: 12px; height: 12px; border-radius: 50%; background: #10B981; display: inline-block;"></span>
            <span class="status-text" id="status-text" style="font-size: 0.9rem; font-weight: 600; color: var(--text-color);">Yükleniyor...</span>
        </div>
        
        <h1 class="panel-title">Dashboard</h1>
        <p class="panel-subtitle">Hoş geldin, <%= user && user.fullname ? user.fullname : 'Kurye' %>! İşte anlık görev durumun.</p>

        <div class="stat-grid" style="margin-bottom: 2rem;">
            <div class="card stat-card">
                <span class="stat-icon">💰</span>
                <span class="stat-label">Bugünkü Kazanç</span>
                <strong class="stat-value">0.00 TL</strong>
            </div>
            <div class="card stat-card">
                <span class="stat-icon">✅</span>
                <span class="stat-label">Bugünkü Teslimat</span>
                <strong class="stat-value">0</strong>
            </div>
        </div>

        <div class="card">
            <div class="card-content">
                <p class="text-muted" style="text-align: center; padding: 2rem;">Yükleniyor...</p>
            </div>
        </div>

    </main>
</div>